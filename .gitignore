# macOS
.DS_Store

# Node / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OpenClaw / local logs
/tmp/

# BlueBubbles proxy logs (local)
bluebubbles/*.log

# If you later decide to keep BlueBubbles credentials out of GitHub,
# add patterns here (e.g. bluebubbles/*.plist)
